import { StyleSheet } from 'react-native';
import type { Theme } from '@/theme/colors';
import { MONO_FONT } from '@/styles/typography';

// eslint-disable-next-line @typescript-eslint/explicit-function-return-type
export const createStyles = (theme: Theme) =>
  StyleSheet.create({
    container: { flex: 1, backgroundColor: theme.background },

    // ── Header ───────────────────────────────────────────────────────────────
    header: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: 12,
      paddingHorizontal: 18,
      paddingBottom: 16,
      backgroundColor: theme.background,
      borderBottomWidth: 1,
      borderBottomColor: theme.borderLight,
    },
    backButton: {
      width: 34,
      height: 34,
      borderRadius: 12,
      backgroundColor: theme.primary,
      alignItems: 'center',
      justifyContent: 'center',
    },
    title: { fontSize: 22, fontWeight: '700', color: theme.text, letterSpacing: -0.2 },
    content: { flex: 1 },
    loadingState: { flex: 1, alignItems: 'center', justifyContent: 'center', gap: 12 },
    loadingText: { fontSize: 14, color: theme.textSecondary },

    // ── Section headers ──────────────────────────────────────────────────────
    sectionHeader: {
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'space-between',
      paddingHorizontal: 18,
      paddingTop: 18,
    },
    sectionHeaderSpaced: { paddingTop: 22 },
    sectionHeaderText: { flex: 1 },
    sectionTitleRow: { flexDirection: 'row', alignItems: 'center', gap: 4 },
    sectionTitle: {
      fontSize: 11,
      fontWeight: '700',
      color: theme.textTertiary,
      textTransform: 'uppercase',
      letterSpacing: 1,
    },
    sectionSubtitle: { fontSize: 11.5, color: theme.textTertiary, marginTop: 2 },
    sectionAddButton: {
      width: 34,
      height: 34,
      borderRadius: 12,
      backgroundColor: theme.primary,
      alignItems: 'center',
      justifyContent: 'center',
      shadowColor: theme.primary,
      shadowOffset: { width: 0, height: 3 },
      shadowOpacity: 0.28,
      shadowRadius: 9,
      elevation: 3,
    },

    // ── Plot cards ───────────────────────────────────────────────────────────
    plotList: { paddingHorizontal: 18, paddingTop: 12, gap: 10 },
    plotCard: {
      flexDirection: 'row',
      alignItems: 'flex-start',
      gap: 12,
      backgroundColor: theme.card,
      borderWidth: 1,
      borderColor: theme.borderLight,
      borderRadius: 16,
      padding: 13,
      shadowColor: theme.shadow,
      shadowOffset: { width: 0, height: 1 },
      shadowOpacity: 0.05,
      shadowRadius: 2,
      elevation: 1,
    },
    plotAvatar: {
      width: 46,
      height: 46,
      borderRadius: 13,
      backgroundColor: theme.primaryLight,
      alignItems: 'center',
      justifyContent: 'center',
    },
    plotAvatarText: {
      fontSize: 14,
      fontWeight: '700',
      color: theme.primary,
      letterSpacing: 0.5,
      fontFamily: MONO_FONT,
    },
    plotBody: { flex: 1, minWidth: 0 },
    plotTitleRow: {
      flexDirection: 'row',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 8,
    },
    plotName: { flex: 1, fontSize: 15.5, fontWeight: '700', color: theme.text },
    plotCents: {
      fontSize: 12,
      fontWeight: '600',
      color: theme.textSecondary,
      fontFamily: MONO_FONT,
    },
    plotMeta: { fontSize: 12, color: theme.textSecondary, marginTop: 2 },
    chipStrip: {
      flexDirection: 'row',
      flexWrap: 'wrap',
      alignItems: 'center',
      gap: 6,
      marginTop: 9,
    },
    chip: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: 4,
      backgroundColor: theme.background,
      borderWidth: 1,
      borderColor: theme.borderLight,
      paddingHorizontal: 8,
      paddingVertical: 3,
      borderRadius: 8,
    },
    chipText: { fontSize: 11, fontWeight: '600', color: theme.textTertiary },
    coordChip: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: 3,
      backgroundColor: theme.primaryLight,
      paddingHorizontal: 8,
      paddingVertical: 3,
      borderRadius: 8,
    },
    coordChipText: {
      fontSize: 11,
      fontWeight: '600',
      color: theme.primary,
      fontFamily: MONO_FONT,
    },
    npkDotRow: { flexDirection: 'row', alignItems: 'center', gap: 2 },
    npkDot: { width: 7, height: 7, borderRadius: 4 },
    plotActions: { gap: 7 },
    editButton: {
      width: 32,
      height: 32,
      borderRadius: 11,
      backgroundColor: theme.backgroundTertiary,
      alignItems: 'center',
      justifyContent: 'center',
    },
    deleteButton: {
      width: 32,
      height: 32,
      borderRadius: 11,
      backgroundColor: theme.errorLight,
      alignItems: 'center',
      justifyContent: 'center',
    },

    // ── Aggregate capacity ───────────────────────────────────────────────────
    capacityRow: {
      flexDirection: 'row',
      justifyContent: 'space-around',
      marginHorizontal: 18,
      marginTop: 12,
      backgroundColor: theme.primaryLight,
      borderWidth: 1,
      borderColor: theme.borderLight,
      borderRadius: 14,
      paddingVertical: 12,
    },
    capacityStat: { alignItems: 'center' },
    capacityValue: {
      fontSize: 17,
      fontWeight: '700',
      color: theme.primary,
      fontFamily: MONO_FONT,
    },
    capacityLabel: { fontSize: 11, color: theme.textSecondary, marginTop: 2 },
    capacityEmpty: {
      fontSize: 12.5,
      color: theme.textTertiary,
      textAlign: 'center',
      marginHorizontal: 18,
      marginTop: 12,
    },

    // ── Section tiles ────────────────────────────────────────────────────────
    sectionGrid: {
      flexDirection: 'row',
      flexWrap: 'wrap',
      paddingHorizontal: 18,
      paddingTop: 12,
      gap: 10,
    },
    sectionTile: {
      width: '48%',
      flexGrow: 1,
      backgroundColor: theme.card,
      borderWidth: 1,
      borderColor: theme.borderLight,
      borderRadius: 14,
      paddingHorizontal: 13,
      paddingVertical: 12,
      shadowColor: theme.shadow,
      shadowOffset: { width: 0, height: 1 },
      shadowOpacity: 0.05,
      shadowRadius: 2,
      elevation: 1,
    },
    sectionTileTop: {
      flexDirection: 'row',
      alignItems: 'flex-start',
      justifyContent: 'space-between',
      gap: 6,
    },
    sectionTileName: {
      flex: 1,
      fontSize: 14.5,
      fontWeight: '700',
      color: theme.text,
      lineHeight: 17,
    },
    sectionTileEdit: {
      width: 26,
      height: 26,
      borderRadius: 9,
      backgroundColor: theme.backgroundTertiary,
      alignItems: 'center',
      justifyContent: 'center',
    },
    sectionTileCount: {
      fontSize: 11.5,
      color: theme.textSecondary,
      marginTop: 8,
      fontFamily: MONO_FONT,
    },

    // ── Empty states ─────────────────────────────────────────────────────────
    emptyStateCard: {
      alignItems: 'center',
      marginHorizontal: 18,
      marginTop: 12,
      paddingVertical: 24,
      paddingHorizontal: 16,
      backgroundColor: theme.card,
      borderRadius: 14,
      borderWidth: 1,
      borderColor: theme.borderLight,
      borderStyle: 'dashed',
    },
    emptyStateIcon: { marginBottom: 10 },
    emptyStateTitle: {
      fontSize: 15,
      fontWeight: '700',
      color: theme.text,
      marginBottom: 4,
      textAlign: 'center',
    },
    emptyStateSubtitle: {
      fontSize: 13,
      color: theme.textSecondary,
      textAlign: 'center',
      lineHeight: 18,
    },

    // ── Saving overlay ───────────────────────────────────────────────────────
    savingOverlay: {
      flex: 1,
      backgroundColor: theme.overlay,
      alignItems: 'center',
      justifyContent: 'center',
      gap: 12,
    },
    savingText: { fontSize: 14, color: theme.textInverse, fontWeight: '600' },
  });
